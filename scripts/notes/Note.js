export const Notes = (Object) => {
    return `
        <section id = ".PrintList">
                <h3>${Object.name}</h3>
                <p> ${Object.id}</p>
                <p> ${Object.noteOfText}</p>
                <p> ${Object.dateOfNote}</p>
                <p> ${Object.suspect}</p>
                </div>
    `
}